# firebase-config.js is intentionally committed — Firebase web API keys are
# public identifiers (not secrets). Security is enforced by Firebase Rules +
# API key HTTP-referrer restrictions in Google Cloud Console.
# js/firebase-config.js  ← intentionally NOT ignored for GitHub Pages deployment

# ─── OS / editor noise ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json
*.log
js/chatbot.src.js
